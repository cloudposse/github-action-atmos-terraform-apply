components:
  terraform:
    foobar-plan-diff:
      metadata:
        component: foobar
      settings:
        github:
          actions_enabled: true
      vars:
        example: blue
        enabled: true         
