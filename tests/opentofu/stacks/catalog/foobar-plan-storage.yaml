components:
  terraform:
    foobar/plan-storage:
      metadata:
        component: foobar
      settings:
        github:
          actions_enabled: true
      vars:
        example: blue
        enabled: true         
